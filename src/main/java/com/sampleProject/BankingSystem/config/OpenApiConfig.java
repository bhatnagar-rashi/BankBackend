package com.sampleProject.BankingSystem.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenApiConfig {
    // Swagger/OpenAPI disabled: springdoc dependency removed to resolve runtime NoSuchMethodError.
    // Re-enable by adding springdoc dependency and restoring @OpenAPIDefinition annotation.
}
